/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
            domains: ['www.aimlay.com', 'www.avanse.com', 'www.npmjs.com', 'encrypted-tbn0.gstatic.com', 'img.freepik.com'],
      }
};

export default nextConfig;
